# Docker-Compose Practice Demonstration

To build the ghost app with multiple containers, use the following command under the ghost directory after installing Docker: 

```bash
docker-compose up -d
```

